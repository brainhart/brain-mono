{
  "name": "ts-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript workspace root",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.8",
    "@types/node": "^22",
    "eslint": "^9",
    "@eslint/js": "^9",
    "typescript-eslint": "^8"
  }
}
